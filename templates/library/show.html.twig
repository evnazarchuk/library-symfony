{% extends 'base.html.twig' %}
{% block title %}Show{% endblock %}
{% block body %}
    <img src="{{ asset('uploads/') ~ book.cover }}" alt="" class="img-thumbnail img-cover-big">
    <h1>{{ book.name }}</h1>
    <p>{{ book.author }}</p>
    <p>{{ book.year }}</p>
{% endblock %}
